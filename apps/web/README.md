# DayStack web app

DayStack is a personal task-execution app for individual professionals. This frontend is
the React, TypeScript, Vite, and shadcn-ui client.

## Run locally

```sh
npm install
npm run dev
```

Run the quality checks with:

```sh
npm test
npm run lint
npm run build
```
